/*
#include <Arduino.h>

// (mal261) sensor pin's output is connected to gpio26
const int sensorPin = 26;

void setup() {
  Serial.begin(115200);
}

void loop() {
  // (mal261) read analog value from sensor pin
  Serial.println(analogRead(sensorPin));
  delay(50); 
}
*/
